version: '3.8'

services:
  engine:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - memory-data:/app/server/memory/data
    restart: unless-stopped

volumes:
  memory-data:
